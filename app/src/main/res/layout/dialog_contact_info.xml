<?xml version="1.0" encoding="utf-8"?>
<!-- app/src/main/res/layout/dialog_contact_info.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- Icono de información actualizado al local moderno -->
    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/ic_info"
        app:tint="#4CAF50"
        android:layout_marginBottom="16dp"/>

    <!-- Título con color adaptativo al tema -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Información de contacto"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnSurface"
        android:layout_marginBottom="12dp"/>

    <!-- Valor de contacto con alto contraste adaptativo -->
    <TextView
        android:id="@+id/tvDialogContactValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:textColor="?attr/colorOnSurface"
        android:textSize="16sp"
        android:textStyle="bold" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDialogClose"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="ENTENDIDO"
        android:textStyle="bold"
        app:backgroundTint="#4CAF50"
        app:cornerRadius="10dp"/>
</LinearLayout>