<?xml version="1.0" encoding="utf-8"?>
<!-- app/src/main/res/layout/item_pet.xml -->
<!-- Tarjeta individual de mascota en la lista de inicio -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.AdoptaPet.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="4dp"
    android:layout_marginBottom="16dp"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Foto de la mascota: proporción 16:9 -->
        <ImageView
            android:id="@+id/ivPetPhoto"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_paw_placeholder"
            android:contentDescription="Foto de la mascota" />

        <!-- Contenido de texto -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Fila: Nombre + Badge de tipo -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Nombre de la mascota -->
                <TextView
                    android:id="@+id/tvPetName"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Max"
                    android:textSize="18sp"
                    android:textColor="@color/text_primary"
                    android:textStyle="bold" />

                <!-- Badge de tipo (Perro / Gato / Otro) -->
                <TextView
                    android:id="@+id/tvPetType"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Perro"
                    android:textSize="12sp"
                    android:textColor="@color/primary"
                    android:background="@drawable/bg_badge"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp" />
            </LinearLayout>

            <!-- Edad -->
            <TextView
                android:id="@+id/tvPetAge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2 años"
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:layout_marginTop="4dp"
                android:drawablePadding="4dp"
                app:drawableStartCompat="@drawable/ic_calendar"
                app:drawableTint="@color/text_secondary" />

            <!-- Descripción breve (máx 2 líneas) -->
            <TextView
                android:id="@+id/tvPetDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Descripción breve de la mascota..."
                android:textSize="14sp"
                android:textColor="@color/text_secondary"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginTop="8dp" />

            <!-- Botón Adoptar -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAdopt"
                style="@style/Widget.AdoptaPet.Button.Accent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/btn_adopt"
                android:layout_marginTop="12dp" />

        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
